<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Forgot Password</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-100 flex items-center justify-center px-4">

  <div class="w-full max-w-md bg-white rounded-lg shadow-lg p-8 space-y-6">
    <h2 class="text-2xl font-bold text-center text-[#c69858]">Forgot Password</h2>

    <% if (error) { %>
      <div class="bg-red-100 text-red-700 px-4 py-2 rounded">
        <%= error %>
      </div>
    <% } %>

    <% if (message) { %>
      <div class="bg-green-100 text-green-700 px-4 py-2 rounded">
        <%= message %>
      </div>
    <% } %>

    <form action="/admin/forgot-password" method="POST" class="space-y-4">
      <div>
        <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
        <input type="email" name="email" id="email" placeholder="Enter your email" required
               class="w-full px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-[#c69858]" />
      </div>

      <button type="submit" class="w-full bg-[#c69858] hover:bg-[#b3864f] text-white font-semibold py-2 rounded transition-colors">
        Submit
      </button>
    </form>

    <p class="text-center text-sm text-gray-600">
      <a href="/admin/login" class="text-[#c69858] hover:underline">Back to Login</a>
    </p>
  </div>

</body>
</html>
